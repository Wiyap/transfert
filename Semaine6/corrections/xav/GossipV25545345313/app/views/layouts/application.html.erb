<!DOCTYPE html>
<html>
  <head>
    <title>TheGossip</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">

  </head>

  <body>
  <a href="<%= root_path %>" class="d-block">
  <header class='jumbotron bg-primary text-white d-flex flex-column align-items-center justify-content-center p-5'>
    <div class="position-absolute start-0 top-0">
    <% if Current.user %>
      <% #button are better than link_to cuz they generate a form with a button and the methode delet %>
      <%= link_to "Log out", log_out_path, method: :delete, class: 'btn btn-outline-light m-2' %>
    <% else %>
      <%= link_to "Sign up", sign_up_path, class: 'btn btn-outline-light m-2' %>
      <%= link_to "Sign in", sign_in_path, class: 'btn btn-outline-light' %>
    <% end %>
    </div>
    <h1 class='text-white'>Bienvenue sur The Gossips Project</h1>
    <div class="col-6 d-flex justify-content-evenly">
      <button class='btn btn-light border border-2 col-5 mt-3 txt-b'><%= link_to :team, users_path %></button>
      <button class='btn btn-light border border-2 col-5 mt-3 txt-b'><%= link_to :gossips, root_path %></button>
    <div>
  </header>
  </a>
  
  <div class="p-5 d-flex flex-column align-items-center">
  <% flash.each do |type, msg| %>
  <div>
    <em><%= msg %></em>
  </div>
<% end %>
    <%= yield %>
  </div>
  </body>
</html>
